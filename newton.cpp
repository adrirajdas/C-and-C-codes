#include<stdio.h>
#include<conio.h>
#define f(x) (x)*(x)*(x)*(x)
int main()
{
	int a[10],n,i;
	printf("enter the range=");
	scanf("%d",&n);
	printf("enter x=");
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
}
